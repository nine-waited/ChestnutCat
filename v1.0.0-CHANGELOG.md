# Chestnut Cat v1.0.0

---

## 简体中文

Chestnut Editor 右下角的板栗猫娘桌宠插件。用 zip 导入到编辑器安装目录的 `plugins/`，不打进编辑器安装包，也不写入笔记库。

### 下载

| 项目 | 说明 |
|------|------|
| **插件包** | `chestnut-cat-1.0.0.zip` |
| **导入** | Chestnut Editor → 设置 → 插件 → 选择该 zip |
| **要求** | Chestnut Editor 0.9.x（插件 zip 导入） |

**构建产物：**

`dist/chestnut-cat-1.0.0.zip`

### v1.0.0 新内容

#### 形象

- 默认使用慵懒鲸鱼脸描改（半垂眼、小横线嘴）
- 眨眼单独用一张基于 idle 的闭眼图，头发、嘴和服饰与 idle 对齐
- 去掉当前套上的呆毛
- 上一版精致猫娘归档在仓库 `assets/expr-v1/`，不进 zip、运行时不用

#### 互动

- 待机每隔 2 秒或 4 秒眨眼
- 偶尔自动换情绪图，并冒出对应慵懒台词
- 单击看今日 Markdown 字数；再点气泡看台词
- 1.2 秒内连点超过 5 次：随机情绪图 + 台词
- 悬停害羞、按住揉头、拖到左半边翻转

#### 安装

- 用 `python scripts/pack-plugin.py` 打 zip
- 只打包 `assets/expr/` 当前套，不含 `expr-v1` 与原猫照片

### 从 0.1.0 升级

- 在编辑器插件页重新导入本 zip（会覆盖安装目录里的 `plugins/chestnut-cat/`）
- 无需改笔记库

---

## English

A board-corner cat pet plugin for Chestnut Editor. Import the zip into the editor install `plugins/` folder — it is not bundled into the editor installer and is not written into the vault.

### Download

| Item | Details |
|------|---------|
| **Plugin zip** | `chestnut-cat-1.0.0.zip` |
| **Install** | Chestnut Editor → Settings → Plugins → pick this zip |
| **Requires** | Chestnut Editor 0.9.x (zip plugin import) |

**Build output:**

`dist/chestnut-cat-1.0.0.zip`

### What's New

#### Art

- Default lazy whale-style faces (half-lidded eyes, small dash mouth)
- Blink uses a closed-eye frame derived from idle; hair, mouth, and clothes stay put
- Ahoge removed from the current set
- The previous moe set is archived at `assets/expr-v1/` (not packed, not loaded)

#### Interaction

- Blink every 2s or 4s while idle
- Occasional emotion poses with lazy one-liners
- Click for today's Markdown word count; click the bubble for a line
- More than 5 clicks in 1.2s: random emotion + line
- Hover shy, hold to pet, drag to the left half to flip

#### Install

- Pack with `python scripts/pack-plugin.py`
- Only `assets/expr/` is packed — not `expr-v1` or the source photo

### Upgrading from 0.1.0

- Re-import this zip in the editor plugin settings (replaces `plugins/chestnut-cat/`)
- No vault migration
